<%= @plant.name %>
<%= @plant.description %>
<%= image_tag @plant.picture.url %>
<%= link_to '編集する', edit_plant_path(@plant) %>
<%= link_to '削除する', @plant, method: :delete, data: { confirm: "削除しますか?" } %>