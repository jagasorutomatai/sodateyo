<div class="row">
    <% @calendars.each do |calendar| %>
        <div class="col-sm-4 mb-3">
            <div class="card calendar_card">
                <% if calendar.picture.url %>
                    <%= image_tag calendar.picture.url %>
                <% end %>
                <div class="card-body">
                    <h5 class="card-title"><%= changeDateFormat(calendar.month) %></h5>
                    <% if calendar.planted_flag %>
                        <span class="badge badge-success">種植え</span>
                    <% end %>
                    <% if calendar.harvested_flag %>
                        <span class="badge badge-success">収穫</span>
                    <% end %>
                    <% if calendar.temperature %>
                        <span class="badge badge-info"><%= "気温:#{calendar.temperature}℃" %></span>
                    <% end %>
                    <p class="card-text text-muted mt-2"><%= calendar.content %></p>
                    <% if current_user?(@post.user) %>
                        <%= link_to "カレンダー編集", edit_calendar_path(calendar), remote: true, class: "btn btn-primary btn-sm btn-block text-white" %>
                        <div id="user-modal" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true"></div>
                    <% end %>
                </div>
            </div>
        </div>
    <% end %>
</div> 